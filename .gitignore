# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Personal data — never commit to GitHub
data/

# Streamlit secrets (contains Supabase credentials)
# Glob, not just secrets.toml: the sandbox uses secrets.sandbox.toml, and this
# repo is public — an exact-filename rule would have let that one through.
.streamlit/secrets*


# Desktop launchers and icons (machine-specific)
*.lnk
*.bat
*.ico

# Misc
map.html
