streamlit==1.42.2
# Pinned to versions contemporary with streamlit 1.42.2 — unpinned, the cloud
# installs 2026-era pyarrow/numpy whose native code crashes the old streamlit
# at startup (process killed, no traceback).
pyarrow<20
numpy<2.3
protobuf<6
Pillow>=9.0
pandas>=2.0
folium>=0.16
streamlit-folium>=0.20
streamlit-geolocation>=0.0.10
sqlalchemy>=2.0
psycopg2-binary>=2.9
altair>=5.0
# Pinned: Authlib 1.6.6 breaks streamlit auth ("400: 'NoneType' object does not
# support item assignment" on /oauth2callback) — see streamlit/streamlit#13461.
Authlib==1.6.5
